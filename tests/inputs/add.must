

fn main() -> int {
    let mut v = 12 in
    let _ = v := 42 in
    let tupl = (13, 165, add(v, 10)) in
    let rev = match tupl {
        (x, _, z) => (z, x),
    } in
    match rev {
        (x, y) => x
    }
}

fn add(x: int, y: int) -> int {
    @intadd(x, y)
}
